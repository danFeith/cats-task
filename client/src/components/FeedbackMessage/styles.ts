import { createUseStyles } from 'react-jss';

export const useFeedbackMessageStyles = createUseStyles({
    success: {
        color: '#27ae60',
        marginTop: 8,
    },
    error: {
        color: '#e74c3c',
        marginTop: 8,
    },
});
