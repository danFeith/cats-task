import { createUseStyles } from "react-jss";

export const useInputFieldStyles = createUseStyles({
    label: {
        display: 'flex',
        flexDirection: 'column',
        fontSize: '1rem',
        color: 'white',
    },
})